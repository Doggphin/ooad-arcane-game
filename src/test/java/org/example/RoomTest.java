package org.example;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class RoomTest {
    @Test
    public void TestRoomClass() {
//        Room room = new Room();
//        assertNotNull(room);
//        Room testRoom = new Room();
//        room.initialize(testRoom, testRoom, testRoom, testRoom, 0);
//        assert(room.getAdjacentRooms()[0] == testRoom);
//        Adventurer testAdventurer = new Adventurer("test", Alignment.GOOD);
//        assert(room.countEntitiesOfAlignment(Alignment.GOOD) == 0);
//        room.addEntity(testAdventurer);
//        assert(room.countEntitiesOfAlignment(Alignment.GOOD) == 1);
    }
}
