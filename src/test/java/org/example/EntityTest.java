package org.example;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class EntityTest {
    @Test
    public void TestEntityClass() {

//        Entity entity = new Entity("Thing", Alignment.GOOD, 10.0);
//        assert(entity.getName().equals("Thing"));
//        assertNotNull(entity);
//
//        Knight entity2 = new Knight("Thing2", Alignment.GOOD);
//        assert(entity2.getName().equals("Thing2"));
//        assertNotNull(entity2);
//
//        Coward entity3 = new Coward("Thing3", Alignment.GOOD);
//        assert(entity3.getName().equals("Thing3"));
//        assertNotNull(entity3);
//
//        Creature entity4 = new Creature("Bob", Alignment.EVIL);
//
//        Demon entity5 = new Demon("Lucifer", Alignment.EVIL);
//        assertNotEquals(entity4, entity5);

        /*
        assert(entity.getHealth() == 5.0);
        assert(!entity.isDead());
        assert(entity.modifyHealth(-1.0) == 4.0);
        assert(entity.getHealth() == 4.0);
        assert(!entity.isDead());
        assert(entity.getName().equals("Thing"));
        */
    }
}
